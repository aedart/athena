import Player from "@aedart/other";

fdescribe('@aedart/mixins', () => {

    // TODO: REMOVE OTHERS PACKAGE AFTER TESTS...

    it('should ...', function () {

        const player = new Player();
        player.fullName()
    });

});
